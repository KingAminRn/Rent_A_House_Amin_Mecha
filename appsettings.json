{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "ItemDbContextConnection": "Data Source=ItemDatabase.db"
  }
}

/*To indicate which DbContext to connect, we add the ConnectionStrings into the appsettings.json (Line9 – Line10):*/
/*This practice separates the configuration from the application code.
  This means you can change the database connection without modifying the code. 
  It also takes advantage of built-in configuration protection features to secure sensitive data (e.g., database name).*/


